package POM_04_Archived_DocumentsPage;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;

import POM_00_BasePage.BasePage;

public class Archived_documents extends BasePage {

	public Archived_documents(WebDriver driver) {
		super(driver);

	}
	
	// 휴지통에 항목이 만들어지는 케이스가 1번 케이스가 됨 = On/Off Case 하나를 먼저 돌려야함 
	// 계약 생성 > 서명 완료 > 휴지통으로 보내기 
	// 계약 생성 > 어떤 서명 상태에서 휴지통으로 보낼지?? 는 나중에 생각하고 일단 화면 부
	
	
}